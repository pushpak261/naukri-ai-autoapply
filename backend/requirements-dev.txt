# Development & CI tooling — not needed to run the agent, only to develop it.
#
# Install with: pip install -r requirements.txt -r requirements-dev.txt

# Testing
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.24.0,<2.0.0
pytest-cov>=5.0.0,<7.0.0

# Linting & formatting
ruff>=0.7.0,<1.0.0
black>=24.0.0,<26.0.0

# Static typing
mypy>=1.11.0,<2.0.0

# API testing
httpx>=0.27.0,<1.0.0

# Pre-commit hooks
pre-commit>=3.8.0,<5.0.0
